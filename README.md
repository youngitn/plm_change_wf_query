# Agile PLM Change Workflow & Part Query System

一個基於 React + Vite + TypeScript 構建的輕量級系統，用於查詢 Agile PLM 中的變更單 (Change Request) 簽核狀態與料號 (Part) 資訊。

## 🚀 技術棧

- **前端框架**: React 19 (TypeScript)
- **建構工具**: Vite
- **UI 框架**: Material UI (MUI) v7
- **狀態管理 & 數據獲取**: TanStack Query (React Query) v5
- **日期處理**: Day.js
- **樣式工具**: Tailwind CSS (用於佈局輔助)
- **API 客戶端**: Axios

## ✨ 核心功能

### 1. 變更單查詢 (Change Management)
- **多維度篩選**: 支援表單類型、事業部、單號、日期區間與關鍵字搜尋。
- **簽核進度可視化**: 點擊單號可彈窗查看詳細的簽核關卡、簽核人及時間。
- **資料匯出**: 支援將搜尋結果匯出為 Excel 報表，並具備完善的錯誤攔截與日期檔名生成。

### 2. 料號資訊查詢 (Part Management)
- **快速搜尋**: 支援料號或描述的模糊查詢。
- **BOM 結構檢視**: 支援展開料號的下階 BOM 清單。

## 🛠️ 開發與部署

### 安裝依賴
```bash
npm install
```

### 啟動開發伺服器
```bash
npm run dev
```

### 專案打包
```bash
npm run build
```

## 📂 專案結構

- `src/features/change-management`: 變更單模組邏輯與 UI。
- `src/features/part-management`: 料號查詢模組。
- `src/components/ui`: 全域通用組件。
- `src/lib/httpClient.ts`: Axios 實例與全域請求/回應攔截器。
- `src/api`: 存放 API 定義。

## 📝 規範

本專案遵循以下開發流程：
1. **Feature Branch**: 任何新功能開發需先建立 `feat/` 分支。
2. **Merge Request**: 完成後合併回 `main` 分支並刪除開發分支。
3. **UI Consistency**: 統一使用 Material UI 作為 UI 組件庫。

---
© 2026 Agile PLM Query System
